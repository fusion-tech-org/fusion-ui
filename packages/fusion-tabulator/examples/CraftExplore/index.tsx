import { Modal } from "@arco-design/web-react";
import { Editor, Frame, Canvas, useNode } from "@craftjs/core";

const TextComponent = ({ text }) => {
  return (
    <div>
      <h2>{text}</h2>

    </div>
  )
}

export const CraftExplore = () => {
  // const { connectors: { drag } } = useNode();

  return (
    <div>

    </div>
  )
};